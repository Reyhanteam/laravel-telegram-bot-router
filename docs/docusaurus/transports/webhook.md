---
title: Webhook
---
# Webhook

Webhook mode receives Telegram POST requests through the configured webhook route. The package provides `php artisan reyhan:set-webhook-route` to register the configured route.

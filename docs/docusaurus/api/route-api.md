---
title: Route API
---
# Route API

The main route API includes `onCommand`, `onText`, `onCallbackQuery`, `onCallback`, update-specific registration methods, middleware registration, groups, conversations, fallback handling, and callback-route generation.

Route registrations expose fluent naming, constraints, chat conditions, permissions, rate limits, and queue configuration.
